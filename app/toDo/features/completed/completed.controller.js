angular
  .module('toDo.completed')
  .controller('CompletedCtrl', CompletedCtrl); // Define controller

function CompletedCtrl() {
  //Expose what's needed to GET all completed tasks
  var vm = this;
}
